"use client";

import { RecipeEditor } from "@/components/ops/producao/RecipeEditor";

/** Criar uma ficha técnica nova. */
export default function NewRecipePage() {
  return <RecipeEditor recipeId={null} />;
}
